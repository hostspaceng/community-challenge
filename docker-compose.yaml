version: '3'

services:
  python-app:
    image: python-project #image from local file
    container_name: python-project #running container name
    ports:
      - "5000:5000" #ports to open for running containers
    environment: #get environmental variables from the .env.sample file
      - ZONE_ID=88212a53b6feba598b197f3508f35b52
      - CF_API_KEY=ab590d1c5d3139416fef3d173ad4267a75a41
      - CF_API_EMAIL=safe@hostspaceng.com
      - VUE_APP_PROXY_URL=http://127.0.0.1:5000/

  vue-app:
    image: vue-project #image from local file
    container_name: vue-project #running container name
    ports:
      - "8080:8080" #ports to open for running containers