version: "3"
services:
  backend:
    build:
        context: . -f backendDockerfile
    ports:
      - "5000:5000"
    networks:
      - my-app-network
  frontend:
    build:
        context: . -f frontendDockerfile
    ports:
      - "8080:80"
    networks:
      - my-app-network

 
networks:
  my-app-network:

    

